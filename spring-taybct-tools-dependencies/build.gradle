description = "Spring TayBct Dependencies"

dependencyManagement {
    imports {
        mavenBom "org.springframework.boot:spring-boot-dependencies:${findProperty('spring-boot.version') ?: '3.5.6'}"
    }
    dependencies {

    }
}
